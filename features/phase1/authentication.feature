# features/phase1/authentication.feature
# ステータス: ドラフト v3

Feature: 投稿認証・管理者認証と日次リセットID
  # US-001: 投稿認証（認証コード + edge-token）
  # US-002: 日次リセットID（IP依存度: 強め）
  # US-003: 管理者ログイン（メール + パスワード）

  一般ユーザーとして、メール登録なしで認証コードを使って投稿したい。
  投稿時は edge-token により認証状態を保持し、同日中は同一IDで識別される。
  管理者はメールアドレスとパスワードでログインしたい。

  # ===========================================
  # 投稿認証（認証コード + edge-token）
  # ===========================================

  Scenario: 未認証ユーザーが投稿すると認証コードが案内される
    Given 未認証のユーザーが投稿フォームから書き込みを送信する
    When サーバーが投稿リクエストを処理する
    Then 認証コード入力ページへの案内が表示される
    And 6桁の認証コードが発行される
    And edge-token Cookie が発行される

  Scenario: 正しい認証コードとTurnstileで認証に成功する
    Given ユーザーが有効な6桁認証コードを持っている
    And ユーザーがTurnstile検証を通過している
    When ユーザーが /auth-code で認証コードを送信する
    Then edge-token が有効化される
    And 投稿可能状態になる

  Scenario: Turnstile検証に失敗すると認証に失敗する
    Given ユーザーが有効な6桁認証コードを持っている
    And ユーザーがTurnstile検証に失敗している
    When ユーザーが /auth-code で認証コードを送信する
    Then 認証エラーメッセージが表示される
    And edge-token は有効化されない

  Scenario: 期限切れ認証コードでは認証できない
    Given ユーザーが有効期限切れの6桁認証コードを持っている
    When ユーザーが /auth-code で認証コードを送信する
    Then 認証エラーメッセージが表示される
    And edge-token は有効化されない

  # ===========================================
  # 日次リセットID（IP依存度: 強め）
  # ===========================================

  Scenario: 同日中は異なるスレッドでも同一の日次リセットIDが表示される
    Given ユーザーが投稿可能状態である
    When スレッド "A" とスレッド "B" にそれぞれ書き込む
    Then 両方の書き込みに同一の日次リセットIDが表示される

  Scenario: 翌日になると日次リセットIDがリセットされる
    Given ユーザーが昨日の日次リセットIDで書き込みを行っている
    When 日付が変更された後に書き込みを行う
    Then 昨日とは異なる新しい日次リセットIDが表示される

  Scenario: Cookie削除後に再認証しても同日・同一回線では同じIDになる
    Given ユーザーが同日中に同一回線から書き込みを行っている
    And ユーザーが edge-token Cookie を削除する
    And ユーザーが認証コードで再認証する
    When 同じ板に再度書き込む
    Then 再認証前と同一の日次リセットIDが表示される

  Scenario: 日付変更のタイミングでIDが混在しない
    Given ユーザーが投稿可能状態である
    And 現在時刻が日付変更直前である
    When 日付変更をまたいで書き込みを行う
    Then 日付変更後の書き込みには新しいIDが適用される
    And 日付変更前の書き込みのIDは変更されない

  # ===========================================
  # 管理者ログイン（メール + パスワード）
  # ===========================================

  Scenario: 管理者が正しいメールアドレスとパスワードでログインする
    Given 管理者アカウントが存在する
    When 管理者が正しいメールアドレスとパスワードを入力してログインする
    Then 管理者セッションが作成される
    And 管理画面にアクセスできる

  Scenario: 管理者が誤ったパスワードでログインすると失敗する
    Given 管理者アカウントが存在する
    When 管理者が誤ったパスワードでログインを試みる
    Then ログインエラーメッセージが表示される
    And 管理者セッションは作成されない
