# features/admin.feature
# ステータス: 承認済み v4
Feature: 管理者機能

  管理者として、不適切なレスやスレッドを削除して掲示板の健全性を最低限維持したい。
  レス削除時は「このレスは削除されました」に置き換わりレス番号は保持される。
  レス削除時には管理者がコメントを入力でき、その内容がシステムレスとして
  スレッドに通知される。コメント未入力時はフォールバックメッセージが表示される。
  スレッド削除時はスレッドと全レスが一覧から消える。
  # US-012: レス・スレッド削除

  # ===========================================
  # レス削除
  # ===========================================

  Scenario: 管理者がコメント付きでレスを削除する
    Given 管理者がログイン済みである
    And スレッド "今日の雑談" にレス >>5 が存在する
    When レス >>5 の削除をコメント "個人情報を含むため削除しました" 付きで実行する
    Then レス >>5 の表示位置に "このレスは削除されました" と表示される
    And レス番号 >>5 は欠番にならず保持される
    And 独立したシステムレスが追加される:
      """
      ★システム
      🗑️ 個人情報を含むため削除しました
      """

  Scenario: 管理者がコメントなしでレスを削除する
    Given 管理者がログイン済みである
    And スレッド "今日の雑談" にレス >>3 が存在する
    When レス >>3 の削除をコメントなしで実行する
    Then レス >>3 の表示位置に "このレスは削除されました" と表示される
    And レス番号 >>3 は欠番にならず保持される
    And 独立したシステムレスが追加される:
      """
      ★システム
      🗑️ レス >>3 は管理者により削除されました
      """

  Scenario: 管理者でないユーザーがレス削除を試みると権限エラーになる
    Given 管理者でないユーザーがログイン済みである
    When レス >>5 の削除を試みる
    Then 権限エラーメッセージが表示される
    And レスは削除されない

  # ===========================================
  # スレッド削除
  # ===========================================

  Scenario: 管理者が指定したスレッドを削除する
    Given 管理者がログイン済みである
    And スレッド "不適切なスレッド" が存在する
    When スレッド "不適切なスレッド" の削除を実行する
    Then スレッドとその中の全レスが削除される
    And スレッド一覧から "不適切なスレッド" が消える

  # ===========================================
  # 削除済みコンテンツの非表示
  # ATK-004-2 / ATK-003-1: soft delete フィルタの対称化
  # ===========================================

  Scenario: 管理者が削除したスレッドはURL直接アクセスでも表示されない
    Given 管理者がログイン済みである
    And スレッド "問題のスレッド" が存在する
    When スレッド "問題のスレッド" の削除を実行する
    Then スレッド "問題のスレッド" をIDで取得しても見つからない
    And スレッド "問題のスレッド" をスレッドキーで取得しても見つからない

  Scenario: 管理者が削除したレスはスレッド閲覧時に「削除されました」と表示される
    Given 管理者がログイン済みである
    And スレッド "今日の雑談" にレス >>5 が存在する
    When レス >>5 の削除をコメントなしで実行する
    Then スレッドのレス一覧で削除済みレスが「このレスは削除されました」と表示される

  Scenario: 削除済みスレッドの再削除は冪等にnot_foundを返す
    Given 管理者がログイン済みである
    And スレッド "二重削除対象" が存在する
    When スレッド "二重削除対象" の削除を実行する
    And スレッド "二重削除対象" の再削除を実行する
    Then 再削除の結果はnot_foundエラーである

  # ===========================================
  # エラーケース
  # ===========================================

  Scenario: 存在しないレスの削除を試みるとエラーになる
    Given 管理者がログイン済みである
    And レス >>999 は存在しない
    When レス >>999 の削除を実行する
    Then エラーメッセージ "指定されたレスが見つかりません" が表示される

  # ===========================================
  # ユーザーBAN
  # See: tmp/feature_plan_admin_expansion.md §1-a
  # US-013: ユーザーBAN / IP BAN
  #
  # 設計意図（BAN の二層構造）:
  #   ユーザーBAN: 警告的措置。通貨リセット等のペナルティが伴う。
  #     Cookie削除で新規IDとして再登録可能（意図的設計）。
  #   IP BAN: 本格的な遮断措置。同一IPからの新規登録・書き込みを完全にブロック。
  #   管理運用: まずユーザーBANで警告 → 改善されなければIP BANにエスカレーション。
  # ===========================================

  Scenario: 管理者がユーザーをBANする
    Given 管理者がログイン済みである
    And ユーザー "UserA" が存在する
    When ユーザー "UserA" をBANする
    Then ユーザー "UserA" のステータスがBAN済みになる

  Scenario: BANされたユーザーの書き込みが拒否される
    Given ユーザー "UserA" がBANされている
    When ユーザー "UserA" がスレッドへの書き込みを試みる
    Then エラーメッセージが表示される
    And レスは追加されない

  Scenario: 管理者がユーザーBANを解除する
    Given 管理者がログイン済みである
    And ユーザー "UserA" がBANされている
    When 管理者がユーザー "UserA" のBANを解除する
    Then ユーザー "UserA" の書き込みが可能になる

  # ===========================================
  # IP BAN
  # See: tmp/feature_plan_admin_expansion.md §1-a
  # ===========================================

  Scenario: 管理者がユーザーのIPをBANする
    Given 管理者がログイン済みである
    And ユーザー "UserA" が存在する
    When ユーザー "UserA" のIPをBANする
    Then IP BANリストに登録される

  Scenario: BANされたIPからの書き込みが拒否される
    Given ユーザー "UserA" のIPがBANされている
    When そのIPからスレッドへの書き込みを試みる
    Then エラーメッセージが表示される
    And レスは追加されない

  Scenario: BANされたIPからの新規登録が拒否される
    Given ユーザー "UserA" のIPがBANされている
    When そのIPから認証を試みる
    Then 認証は拒否される

  Scenario: 管理者がIP BANを解除する
    Given ユーザー "UserA" のIPがBANされている
    When 管理者がそのIP BANを解除する
    Then そのIPからの書き込みが可能になる

  # ===========================================
  # 通貨付与
  # See: tmp/feature_plan_admin_expansion.md §1-b
  # US-014: 通貨付与
  # ===========================================

  Scenario: 管理者が指定ユーザーに通貨を付与する
    Given 管理者がログイン済みである
    And ユーザー "UserA" の通貨残高が 50 である
    When ユーザー "UserA" に通貨 100 を付与する
    Then ユーザー "UserA" の通貨残高が 150 になる

  Scenario: 管理者でないユーザーが通貨付与を試みると権限エラーになる
    Given 管理者でないユーザーがログイン済みである
    When 通貨付与APIを呼び出す
    Then 権限エラーメッセージが表示される

  # ===========================================
  # ユーザー管理
  # See: tmp/feature_plan_admin_expansion.md §1-c
  # US-015: ユーザー管理
  # ===========================================

  Scenario: 管理者がユーザー一覧を閲覧できる
    Given 管理者がログイン済みである
    And ユーザーが5人登録されている
    When ユーザー一覧ページを表示する
    Then ユーザーが一覧表示される
    And 各ユーザーのID、登録日時、ステータス、通貨残高が表示される

  Scenario: 管理者が特定ユーザーの詳細を閲覧できる
    Given 管理者がログイン済みである
    And ユーザー "UserA" が過去に3件の書き込みを行っている
    When ユーザー "UserA" の詳細ページを表示する
    Then ユーザーの基本情報（ステータス、通貨残高、ストリーク）が表示される
    And 書き込み一覧が表示される

  Scenario: 管理者がユーザーの書き込み履歴を確認できる
    Given 管理者がユーザー "UserA" の詳細ページを表示している
    Then 管理者画面でも各書き込みのスレッド名、本文、書き込み日時が含まれる

  # ===========================================
  # ダッシュボード
  # See: tmp/feature_plan_admin_expansion.md §1-d
  # US-016: ダッシュボード
  # ===========================================

  Scenario: 管理者がダッシュボードで統計情報を確認できる
    Given 管理者がログイン済みである
    When ダッシュボードを表示する
    Then 総ユーザー数が表示される
    And 本日の書き込み数が表示される
    And アクティブスレッド数が表示される
    And 通貨流通量が表示される

  Scenario: 管理者が統計情報の日次推移を確認できる
    Given 管理者がログイン済みである
    And 過去7日分の日次統計が記録されている
    When ダッシュボードの推移グラフを表示する
    Then 日付ごとの統計推移が確認できる

  # ===========================================
  # 課金ステータス管理
  # 課金トラブル時の対応として、管理者がユーザーの有料/無料を切り替える。
  # 無料への変更時のテーマ・フォント影響は theme.feature で定義済み。
  # ===========================================

  Scenario: 管理者がユーザーを有料ステータスに変更する
    Given 管理者がログイン済みである
    And ユーザー "UserA" は無料ユーザーである
    When ユーザー "UserA" を有料ステータスに変更する
    Then ユーザー "UserA" が有料ユーザーになる

  Scenario: 管理者がユーザーを無料ステータスに変更する
    Given 管理者がログイン済みである
    And ユーザー "UserA" は有料ユーザーである
    When ユーザー "UserA" を無料ステータスに変更する
    Then ユーザー "UserA" が無料ユーザーになる
