# features/user_registration.feature
# ステータス: レビュー中ドラフト v2
#
# 設計背景:
#   Phase 1-2 では edge-token + Turnstileによる「仮ユーザー」のみが存在した。
#   Cookie 喪失・端末変更時にユーザーの同一性が失われる不安定さを解消するため、
#   メールアドレスまたは Discord アカウントによる「本登録」機能を導入する。
#
# 用語定義:
#   仮ユーザー: edge-token + Turnstileで認証済み、本登録未完了のユーザー
#   本登録ユーザー: メールアドレスまたは Discord アカウントを紐付け済みのユーザー
#   PAT: パーソナルアクセストークン。本登録完了時に自動発行される専ブラ連携用トークン
#
# 関連ドキュメント:
#   設計詳細 → D-08 docs/architecture/components/user-registration.md
#   状態遷移 → D-05 docs/specs/user_registration_state_transitions.yaml
#   既存認証 → features/authentication.feature
#   既存マイページ → features/mypage.feature
#   専ブラ互換 → features/specialist_browser_compat.feature
#
# 既存 BDD への影響:
#   mypage.feature の「無料ユーザーが課金ボタンで有料ステータスに切り替わる」は
#   本機能により本登録ユーザーのみ課金可能に制約される（本 feature が優先）。

Feature: 本登録・ログイン・専ブラ連携トークン（PAT）

  仮ユーザーとして、メールアドレスまたは Discord アカウントで「本登録」を行い、
  Cookie 喪失や端末変更時でもログインにより同一ユーザーとして復帰したい。
  本登録後も仮ユーザー時代のデータ（通貨・書き込み履歴・ストリーク等）は
  すべて引き継がれる。

  専ブラユーザーとして、パーソナルアクセストークン（PAT）をメール欄に設定する
  ことで、新規端末でも同一ユーザーとして書き込みたい。PAT を設定しておけば
  Cookie 喪失時だけでなく Cookie が別ユーザーへ分岐した場合でも、PAT を正として
  同一ユーザーへ復帰したい。

  # ===========================================
  # 本登録（メール認証）
  # ===========================================

  Scenario: 仮ユーザーがメールアドレスとパスワードで本登録を申請する
    Given 仮ユーザーがマイページを表示している
    When メールアドレスとパスワードを入力して本登録を申請する
    Then メールアドレスに確認リンクが送信される
    And 本登録は確認待ち状態になる

  Scenario: メール確認リンクをクリックして本登録が完了する
    Given 仮ユーザーが本登録申請済みで確認メールを受信している
    When メール内の確認リンクをクリックする
    Then 本登録が完了する
    And マイページにアカウント種別 "本登録ユーザー" と表示される
    And PATが自動発行されマイページに表示される

  Scenario: 既に使用されているメールアドレスでは本登録できない
    Given 別のユーザーがそのメールアドレスで本登録済みである
    When 仮ユーザーが同じメールアドレスで本登録を申請する
    Then エラーメッセージが表示される
    And 本登録は完了しない

  # ===========================================
  # 本登録（Discord 連携）
  # ===========================================

  Scenario: 仮ユーザーが Discord アカウントで本登録する
    Given 仮ユーザーがマイページを表示している
    When Discord で本登録ボタンを押す
    And Discord 認可画面で許可する
    Then 本登録が完了する
    And マイページにアカウント種別 "本登録ユーザー" と表示される
    And 認証方法に "Discord" と表示される
    And PATが自動発行されマイページに表示される

  # ===========================================
  # データ引き継ぎ
  # ===========================================

  Scenario: 本登録後に仮ユーザー時代の通貨残高が引き継がれる
    Given 仮ユーザーの通貨残高が 150 である
    When 本登録を完了する
    Then 通貨残高は 150 のままである

  Scenario: 本登録後に仮ユーザー時代の書き込み履歴が引き継がれる
    Given 仮ユーザーが過去に5件の書き込みを行っている
    When 本登録を完了する
    Then マイページの書き込み履歴に5件すべてが表示される

  Scenario: 本登録後に仮ユーザー時代のストリークが引き継がれる
    Given 仮ユーザーの連続書き込み日数が 7 である
    When 本登録を完了する
    Then 連続書き込み日数は 7 のままである

  # ===========================================
  # edge-token 継続性
  # ===========================================

  Scenario: 本登録後も既存の edge-token で書き込みできる
    Given 仮ユーザーが edge-token で書き込み可能である
    When 本登録を完了する
    And 同じデバイスから書き込みを行う
    Then 書き込みは本登録前と同じ edge-token で処理される

  Scenario: 専ブラ認証リンクから通常ブラウザで認証した後に同一ユーザーで本登録導線へ進める
    Given 専ブラの未認証書き込みに対して認証ページURLが返されている
    And そのURLには専ブラユーザーの edge-token が含まれている
    When 通常ブラウザで認証ページを開いて Turnstile 認証を完了する
    Then 同一 user_id に対する web 用 edge-token Cookie が発行される
    And マイページまたは本登録ページへ遷移できる
    And 新しい仮ユーザーは作成されない

  Scenario: Cookie 削除後に非ログイン状態で書き込むと別人として扱われる
    Given 本登録ユーザーが edge-token Cookie を削除する
    When ログインせずに書き込みを行う
    Then 新しい仮ユーザーとして認証フローが開始される
    And 本登録ユーザーのデータにはアクセスできない

  Scenario: Cookie 削除後にログインすると同一ユーザーに復帰する
    Given 本登録ユーザーが edge-token Cookie を削除する
    When メールアドレスとパスワードでログインする
    Then 新しい edge-token が発行される
    And 本登録ユーザーのデータ（通貨・履歴等）にアクセスできる

  # ===========================================
  # ログイン（Web ブラウザ）
  # ===========================================

  Scenario: 本登録ユーザーがメールアドレスとパスワードでログインする
    Given 本登録ユーザー（メール認証）が新しいデバイスを使用している
    When メールアドレスとパスワードでログインする
    Then edge-token Cookie が発行される
    And 本登録ユーザーのデータにアクセスできる

  Scenario: 本登録ユーザーが Discord アカウントでログインする
    Given 本登録ユーザー（Discord 連携）が新しいデバイスを使用している
    When Discord アカウントでログインする
    Then edge-token Cookie が発行される
    And 本登録ユーザーのデータにアクセスできる

  Scenario: ログイン後も旧デバイスの edge-token は有効なままである
    Given 本登録ユーザーがデバイスAで書き込み可能である
    When デバイスBからログインする
    Then デバイスAの edge-token は引き続き有効である
    And 両方のデバイスから同一ユーザーとして書き込みできる

  Scenario: 誤ったパスワードではログインできない
    Given 本登録ユーザー（メール認証）のアカウントが存在する
    When 誤ったパスワードでログインを試みる
    Then ログインエラーメッセージが表示される

  # ===========================================
  # パスワード再設定
  # ===========================================

  Scenario: 本登録ユーザーがパスワード再設定を申請する
    Given 本登録ユーザー（メール認証）のアカウントが存在する
    When パスワード再設定を申請する
    Then メールアドレスにパスワード再設定リンクが送信される

  Scenario: パスワード再設定リンクから新しいパスワードを設定する
    Given 本登録ユーザーがパスワード再設定メールを受信している
    When メール内の再設定リンクをクリックする
    And 新しいパスワードを入力して確定する
    Then パスワードが更新される
    And 新しいパスワードでログインできる

  Scenario: 未登録のメールアドレスでパスワード再設定を申請してもエラーを明かさない
    When 未登録のメールアドレスでパスワード再設定を申請する
    Then 登録済みの場合と同じ応答が返る
    # セキュリティ: メールアドレスの登録有無を外部から判別できないようにする

  # ===========================================
  # ログアウト
  # ===========================================

  Scenario: ログアウトすると書き込みに再認証が必要になる
    Given 本登録ユーザーがログイン済みである
    When ログアウトする
    Then edge-token Cookie が削除される
    And 書き込みを行うと認証フローが開始される

  # ===========================================
  # PAT（専ブラ連携トークン）
  # ===========================================

  Scenario: 本登録完了時に PAT が自動発行される
    Given 仮ユーザーがマイページを表示している
    When 本登録を完了する
    Then マイページに "#pat_<PAT>" 形式の PAT と専ブラでの設定方法が表示される

  Scenario: マイページで PAT を確認できる
    Given 本登録ユーザーがマイページを表示している
    Then "#pat_<PAT>" 形式の PAT が常に表示されている
    And PAT の素のトークン文字列は別表示されない
    And PAT の最終使用日時が表示される

  Scenario: 専ブラの mail 欄に PAT を設定して書き込みできる
    Given 本登録ユーザーが PAT を取得している
    And 専ブラで未認証の新しいデバイスを使用している
    When bbs.cgi のメール欄に "#pat_<PAT>" を含めて POST する
    Then PAT が検証される
    And edge-token Cookie が発行される
    And 書き込みが本登録ユーザーとしてスレッドに追加される
    And メール欄の PAT は書き込みデータに含まれない

  Scenario: PAT 入力時に同一ユーザーの Cookie が有効ならその Cookie を再利用する
    Given 専ブラで PAT による edge-token Cookie が発行済みである
    And メール欄に PAT が設定されたままである
    When bbs.cgi に書き込みを POST する
    Then PAT の所有者と edge-token Cookie の所有者が照合される
    And 同一ユーザーであるため既存の edge-token Cookie が再利用される

  Scenario: PAT 入力時に別ユーザーの Cookie が有効でも PAT を正として上書きする
    Given 専ブラの edge-token Cookie が別ユーザーに紐付いている
    And メール欄に本来の本登録ユーザーの PAT が設定されている
    When bbs.cgi に書き込みを POST する
    Then PAT が検証される
    And PAT の所有者がその書き込みの投稿者として採用される
    And 専ブラの edge-token Cookie は PAT 所有者向けの新しい値で上書きされる
    And 書き込みは PAT 所有者として処理される

  Scenario: Cookie 喪失時に mail 欄の PAT で自動復帰する
    Given 専ブラの edge-token Cookie が失効している
    And メール欄に PAT が設定されたままである
    When bbs.cgi に書き込みを POST する
    Then PAT で認証され新しい edge-token Cookie が発行される
    And 本登録ユーザーとして書き込みが処理される

  Scenario: 無効な PAT は有効な Cookie があっても優先して拒否される
    Given 専ブラの edge-token Cookie が有効である
    And メール欄に無効な PAT が設定されている
    When bbs.cgi に書き込みを POST する
    Then 認証エラーが発生する
    And 書き込みは処理されない

  Scenario: PAT を再発行すると旧 PAT が無効になる
    Given 本登録ユーザーがマイページで PAT を再発行する
    When 旧 PAT を使って専ブラから書き込みを試みる
    Then 認証エラーが発生する
    And 新しい PAT を使えば書き込みできる

  Scenario: 仮ユーザーには PAT が表示されない
    Given 仮ユーザーがマイページを表示している
    Then PAT セクションには本登録を促すメッセージが表示される

  Scenario: 無効な PAT では書き込みが拒否される
    When bbs.cgi のメール欄に無効な PAT を含めて POST する
    Then レスポンスの title タグに "ＥＲＲＯＲ" が含まれる

  # ===========================================
  # 課金制約（本登録前提）
  # ===========================================
  # mypage.feature の「課金ボタン」シナリオに対する追加制約。
  # 本登録ユーザーのみ課金（有料ステータス切り替え）が可能。

  Scenario: 仮ユーザーは課金できない
    Given 仮ユーザーがマイページを表示している
    Then 課金ボタンは無効化されている
    And 本登録が必要である旨のメッセージが表示される

  Scenario: 本登録済みの無料ユーザーは課金できる
    Given 本登録済みの無料ユーザーがマイページを表示している
    When 課金ボタンを押す
    Then 有料ユーザーステータスに切り替わる

  # ===========================================
  # マイページ表示
  # ===========================================

  Scenario: 仮ユーザーのマイページに本登録案内が表示される
    Given 仮ユーザーがマイページを表示している
    Then アカウント種別に "仮ユーザー" と表示される
    And メール認証と Discord 連携による本登録への案内が表示される

  Scenario: 本登録ユーザーのマイページにアカウント種別と認証方法が表示される
    Given 本登録ユーザー（メール認証）がマイページを表示している
    Then アカウント種別に "本登録ユーザー" と表示される
    And 認証方法に "メール" と表示される
