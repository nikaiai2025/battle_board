# D-10 BDDテスト戦略書

> ステータス: 承認済み
> 作成日: 2026-03-12
> 対象スコープ: Phase 1 全体（Phase 2以降も基本方針を継承）

---

## 1. テストレベルの決定: サービス層テスト

**結論:** サービス層の公開関数を直接呼び出す。APIルート（Next.js Route Handler）は経由しない。

**理由:**
- APIルートは「リクエスト受付 → Service呼び出し → レスポンス整形」の薄いアダプターであり、ビジネスロジックを持たない
- HTTPサーバーの起動・リクエスト生成が不要になり、テスト実行速度とセットアップの単純さが向上する
- vitestの単体テストが同じサービス層を対象としており、テスト対象の粒度が一致する
- featureファイルのシナリオは全てビジネスロジックの振る舞いを記述しており、HTTPステータスコードやヘッダーの検証を含まない

**注意:** サービス間の内部呼び出し連鎖（例: PostService → IncentiveService）はモックせず、結合した状態でテストする。BDDシナリオが検証するのはユーザーから見た振る舞いの結果であり、サービス間の呼び出し構造ではない。

---

## 2. 外部依存のモック戦略

**方針:** リポジトリ層のモジュールをインメモリ実装に差し替える。実DB接続は行わない。

**理由:**
- Supabase（PostgreSQL）への実接続はCI環境での信頼性・速度に問題がある
- vitestでは `vi.mock()` を使っているが、Cucumber.jsの実行環境（ts-node/CommonJS）では利用不可
- Cucumber.js v12ではESMサポートが限定的であるため、CommonJS互換の手法が安全

**インメモリ実装の設計方針:**
- 各リポジトリのエクスポート関数と同一シグネチャの関数を持つオブジェクトを用意する
- データは `Map<string, T>` や配列で保持し、Worldのライフサイクル（シナリオ単位）でリセットする
- DB固有動作（`ON CONFLICT DO NOTHING`、楽観的ロック等）は、インメモリ実装内で同等のロジックを再現する
- モック機構の具体的な実装方式（requireキャッシュ書き換え、tsconfig-paths誘導等）はコーダーが選定する。ただしプロジェクト内で1方式に統一すること

**ライフサイクル:**
- `BeforeAll`: モジュール差し替えをインストール
- `Before`（各シナリオ前）: インメモリデータをクリア
- `After`（各シナリオ後）: 必要に応じたクリーンアップ（時刻スタブの復元等）

---

## 3. Cucumber World 設計

**Worldが保持すべき状態カテゴリ:**

| カテゴリ | 説明 | 用途例 |
|---|---|---|
| 現在のユーザー | userId, edgeToken, ipHash, isPremium, username | 「ユーザーがログイン済みである」の状態管理 |
| 名前付きユーザーマップ | `Map<string, UserContext>` | 「ユーザー "UserA"」「ユーザー "UserB"」の複数ユーザー管理 |
| 現在のスレッド | threadId, threadTitle | 「スレッド "今日の雑談" を閲覧している」の状態管理 |
| 最後の操作結果 | lastResult, lastError | Then ステップでのアサーション対象 |
| 時刻制御 | currentTime (Date) | 日付変更・経過時間のシミュレーション |

**設計原則:**
- Worldのプロパティは型付きで定義する（anyを避ける）
- 各シナリオの独立性を保証するため、Beforeフックで全状態をリセットする
- テスト用エンティティの生成ヘルパーをWorldのメソッドとして提供する

---

## 4. ディレクトリ構成とファイル分割方針

```
features/
  support/
    world.ts              # World クラス定義
    hooks.ts              # BeforeAll / Before / After フック
    mock-installer.ts     # リポジトリモジュール差し替えの機構
    in-memory/            # インメモリリポジトリ実装群（1リポジトリ = 1ファイル）
  step_definitions/
    common.steps.ts       # 複数featureで共有されるステップ
    {feature}.steps.ts    # feature固有のステップ（1 feature = 1ファイル）
```

**分割基準:**
- 1 feature = 1 stepsファイルが原則
- 3つ以上のfeatureで使われるステップは common.steps.ts に集約する
- 2つのfeatureでのみ共有されるステップは、先に定義されたfeatureのstepsファイルに置く（重複定義エラーを避ける）

---

## 5. 時刻制御の方針

多くのシナリオが日付・時間に依存する（日次リセットID、ストリーク、低活性スレッド判定、時間内ボーナス等）。時刻依存テストはFlakyテスト（実行タイミングにより結果が変わる不安定テスト）の最大の原因であり、以下の原則を厳守する。

### 5.1 時計凍結の原則

**方針:** テスト中は実時間に一切依存せず、凍結した時計のみを使用する。

- `Date.now` をグローバルにスタブ化し、World経由で任意の時刻に設定・進行する
- Beforeフックでオリジナルの `Date.now` を保存し、Afterフックで復元する
- sinon の `useFakeTimers` やカスタム実装のどちらでもよいが、1つの方式に統一すること

### 5.2 相対時刻の禁止

テストデータの時刻を `Date.now() - offset` で算出してはならない。テスト実行中に実時間が経過するため、ステップ間でミリ秒単位の不整合が生じFlakyテストの原因になる。

```typescript
// NG: 実行タイミングでミリ秒がずれる
const pastTime = new Date(Date.now() - 25 * 60 * 60 * 1000)

// OK: 時計を固定時刻に設定してからデータを作る
world.setCurrentTime(new Date('2026-03-12T10:00:00+09:00'))
// この時点で Date.now() は固定値を返すため、サービス内の new Date(Date.now()) も同じ値になる
```

**時刻依存シナリオの標準パターン:**

1. 時計を基準時刻 T に凍結する
2. 時計を過去（T - N時間）に設定し、事前データを作成する
3. 時計を T に戻し、テスト対象の操作を実行する
4. 必要に応じて時計を T + M 分に進め、後続操作を実行する
5. 結果をアサートする

### 5.3 サービス層の時刻取得統一

サービス層で現在時刻を取得する際は `new Date(Date.now())` を使用し、`new Date()` を直接使用しない。`Date.now` のスタブがサービス内部の時刻生成にも反映されることを保証するためである。

### 5.4 テストデータ作成の優先順位

時刻依存の事前データ（過去のレス、スレッドの最終書き込み時刻等）を作成する際は、以下の優先順位に従う:

1. **サービス経由での作成**（推奨）: 時計を過去に設定し、通常のサービスAPI（PostService.createPost等）で作成する。サービスの実際の動作と一致するため最も信頼性が高い
2. **インメモリリポジトリの直接操作**（許容）: サービス経由では作成が困難な状態（途中状態、異常状態等）に限りリポジトリの `_insert` 等を使用する。この場合もcreatedAt等の時刻フィールドは凍結時計の値を使うこと

---

## 6. スコープ外シナリオの扱い方針

featureファイルは変更禁止（CLAUDE.md禁止事項）であるため、タグの追加によるスキップはできない。

**方針:**
- 対象外のfeatureファイルは cucumber.js の `paths` で除外する
- 対象featureファイル内のスコープ外シナリオは `name` オプション（正規表現 negative match）で除外する
- 具体的な除外対象はスプリント固有の実装ガイドで定義する

---

## 7. テストピラミッドと多層テスト戦略

> Phase 2以降に適用。Phase 1はサービス層テスト（§1）+ 単体テスト（Vitest）で完結していた。

### 7.1 全体像

```
     ┌─────────────────────┐
     │  本番 Smoke（§14）    │  本番URL — 読み取り専用・人間監督必須（11本）
    ─┼─────────────────────┼─
    │      E2E              │  Playwright — 重要フローのみ（数本〜10本）
   ──┼──────────────────────┼──
   │    API テスト           │  HTTPレベル検証 — 専ブラ互換・Cookie等（10〜20本）
  ───┼───────────────────────┼───
  │   CF Smoke              │  wrangler dev — 互換性スモークテスト（7本程度・固定）
 ────┼────────────────────────┼────
 │    統合テスト              │  Supabase Local実DB — 既存BDDシナリオを流用
─────┼─────────────────────────┼─────
│   BDD サービス層            │  InMemoryリポジトリ — 高速・開発中の主力（既存）
──────┼──────────────────────────┼──────
│    単体テスト (Vitest)       │  純粋関数・ドメインモデル（既存）
───────┴──────────────────────────┴───────
```

**原則:** 下層ほどテスト数を多く、上層ほど厳選する。各層の責務を明確に分離し、重複検証を避ける。

### 7.2 各層の責務と検証対象

| 層 | 検証対象 | 検証しないこと |
|---|---|---|
| 単体テスト | 純粋関数の入出力、ドメインルール | 外部依存、統合動作 |
| BDDサービス層 | ビジネスロジックの振る舞い、サービス間連携 | SQL、HTTP、UI |
| 統合テスト | SQL・RLS・マイグレーション・リポジトリ層の実動作 | HTTP、UI |
| CF Smoke | Workers Runtime互換性（Shift_JIS, crypto, Buffer, rewrites, SSR） | ビジネスロジック、Cookie、認証フロー |
| APIテスト | ルーティング、Cookie処理、Content-Type、エンコーディング | ビジネスロジック（サービス層で検証済み） |
| E2E（フロー検証） | 認証フロー、複数ユーザー操作、画面遷移の一連の流れ | 個別のビジネスルール |
| E2E（スモーク） | 全ページの到達性、全リンク・ボタンの操作可能性 | ビジネスロジック、データの正確性 |
| 本番 Smoke | 本番環境の到達性・応答正常性（§14） | ビジネスロジック、データの正確性、書き込み操作 |

### 7.3 BDDシナリオの検証層マッピング

BDDシナリオ（D-03）は振る舞いの正本であり、UIの振る舞いも含めて記述してよい。ただし全シナリオがCucumber（サービス層テスト）で検証できるわけではない。サービス層で検証不可能なシナリオは、テストピラミッドの適切な層で代替検証する。

#### 7.3.1 マッピングルール

シナリオの性質に応じて検証層を決定する。

| シナリオの性質 | 検証層 | Cucumberでの扱い |
|---|---|---|
| ビジネスロジック（状態遷移、計算、コマンド処理等） | BDDサービス層 | 通常実行 |
| DOM/CSS表示（文字色、表示切替、レイアウト等） | Vitestコンポーネントテスト | pending |
| ブラウザ固有動作（ナビゲーション、認証フロー等） | E2E | pending |
| インフラ制約（エンコーディング、HTTP属性等） | CF Smoke / APIテスト | pending |

#### 7.3.2 Cucumber pending運用ルール

- ステップ定義は `return "pending"` で実装し、Cucumber実行時にスキップさせる
- ステップ定義のコメントに pending理由と代替テストのファイルパスを記載する
- featureファイル自体は変更しない（CLAUDE.md禁止事項）

```typescript
// Web限定シナリオ — Cucumberサービス層では検証不可
// 代替検証: src/__tests__/app/(web)/thread/eliminated-bot-display.test.tsx
Then('撃破済みボットの過去のレスは目立たない文字色で表示される',
  async function () { return "pending"; }
);
```

#### 7.3.3 代替テスト側のトレーサビリティ規約

代替テストのファイル先頭に、対応するBDDシナリオへの参照を記載する。BDDシナリオの文言が変更された場合、代替テスト側も追従すること。

```typescript
/**
 * @feature bot_system.feature
 * @scenario 撃破済みボットのレスはWebブラウザで目立たない表示になる
 * @scenario 撃破済みボットのレス表示をトグルで切り替えられる
 */
describe('撃破済みボット表示', () => { ... });
```

#### 7.3.4 pendingシナリオの管理

- 代替テストが未作成のpendingシナリオは技術的負債として扱う
- UIコンポーネント実装時に、対応する代替テストの作成を必須とする
- E2Eスモーク（§10.5）の拡充により代替テストが包含される場合、Vitestコンポーネントテスト側は削除してよい（二重検証の回避）

---

## 8. 統合テスト方針

### 8.1 基本方針

既存BDDシナリオ（`.feature`）とステップ定義をそのまま流用し、リポジトリの注入先をInMemory実装からSupabase Local実装に切り替えて実行する。

**メリット:** 新規テストコードの作成がほぼ不要。既存BDDシナリオが自動的に統合テストになる。

### 8.2 切替の仕組み

- テストプロファイル or 環境変数（例: `TEST_DB=supabase`）でリポジトリ実装を分岐する
- `BeforeAll` フックで判定し、InMemory / Supabase のいずれかをインストールする
- ステップ定義側は変更不要（リポジトリのインターフェースが同一であるため）

### 8.3 前提条件

- Docker Desktop + Supabase Local が起動していること（`npx supabase start`）
- マイグレーションが適用済みであること（`npx supabase db push`）
- テスト用の `.env` に `TURNSTILE_SECRET_KEY` を設定しないこと（§10参照）

### 8.4 データライフサイクル

- `Before`（各シナリオ前）: テスト用テーブルのTRUNCATE
- シナリオ間の独立性はインメモリ版と同様に保証する

---

## 9. APIテスト方針

### 9.1 基本方針

Next.jsサーバー（`next build && next start` または `next dev`）を起動し、fetchでAPIエンドポイントを叩くHTTPレベルのテスト。**全APIに書くのではなく、サービス層テストでカバーできない検証が必要な箇所に限定する。**

### 9.2 対象領域

| 対象 | 検証内容 | 理由 |
|---|---|---|
| 専ブラ互換API | Shift_JISエンコーディング、DAT形式、subject.txt、bbs.cgi | バイナリレベルの正確性はサービス層テストでは検証不可 |
| 認証API | Cookie設定（Secure, HttpOnly, SameSite）、レスポンスヘッダー | HTTP属性はサービス層では検証不可 |
| エラーレスポンス | HTTPステータスコード、エラーボディ形式 | クライアントとの契約 |

### 9.3 対象外

- ビジネスロジックの検証（サービス層テストで十分カバーされている）
- 正常系のCRUD操作（APIルートは薄いアダプターであり、追加の検証価値が低い）

---

## 10. E2Eテスト方針

E2Eテストは以下の2分類で構成する。

| 分類 | 目的 | 配置先 | 数量目標 |
|---|---|---|---|
| **フロー検証テスト** | 認証→操作→結果確認の一連のユーザーフローが完結すること | `e2e/` 直下 | 数本〜10本 |
| **ナビゲーションスモークテスト** | 全ページの到達性、全リンク・ボタンの操作可能性 | `e2e/smoke/` | ページ数に比例（各ページ1テスト） |

**BDDとの役割分担:** BDDサービス層テストは「振る舞いの正しさ」を検証する。E2Eスモークテストは「画面が到達可能であり、UI要素が操作可能であること」を検証する。両者は相互補完の関係にあり、検証領域が重複しない。

### 10.1 基本方針

Playwright + Supabase Localを使用したブラウザ自動テスト。**作成・維持コストが最も高いため、重要な一連のフローのみを対象とする。** 個別のビジネスルール検証には使用しない。

### 10.2 ツール

- **Playwright**（推奨）: クロスブラウザ対応、Next.jsとの親和性が高い

### 10.3 フロー検証テストの選定基準

以下のいずれかに該当するフローのみE2Eテストを作成する:
- 認証 → 操作 → 結果確認の一連のフロー（複数APIをまたぐ）
- 複数ユーザー間の同時操作シナリオ
- 人間が手動確認の代替として指定したシナリオ

### 10.4 フロー検証テストの数量目標

数本〜10本程度。増やすよりも、各テストが重要なフローを確実にカバーすることを優先する。

### 10.5 ナビゲーションスモークテスト

#### 10.5.1 目的と背景

過去に「ページは存在するがリンクが設置されておらず到達不可」という問題が発生した。BDDサービス層テストではUIの到達性を検証できないため、E2Eスモークテストで「全ページにアクセスでき、全ボタン・リンクがエラーなく動作する」ことを保証する。

#### 10.5.2 対象ページの特定方法

`src/app/` 配下の全 `page.tsx` が検証対象となる。新規ページ追加時に対応するスモークテストも追加すること（増減基準は§10.5.5を参照）。

#### 10.5.3 各ページでの検証項目

各ページについて以下を検証する。ビジネスロジックの正しさやデータの正確性は検証しない。

| 検証項目 | 内容 |
|---|---|
| **アクセス可能性** | HTTPステータス200で応答する（404/500でない） |
| **JSエラーなし** | `page.on('pageerror')` でコンソールエラーが発生しない |
| **主要UI要素の存在** | ページ固有のランドマーク要素（ヘッダー、メインコンテンツ領域等）が表示される |
| **リンク・ボタンの操作可能性** | ページ内の主要リンクがクリック可能であり、遷移先が404/500でない |

#### 10.5.4 ファイル構成

```
e2e/
  smoke/
    navigation.spec.ts    # 全ページの到達性・ナビゲーション検証
```

ページ数が増加し1ファイルが肥大化した場合は、機能領域単位（例: `smoke/admin.spec.ts`, `smoke/thread.spec.ts`）に分割してよい。

#### 10.5.5 増減基準

- **追加:** `src/app/` に新しい `page.tsx` を追加した場合、対応するスモークテストケースを必ず追加する
- **削除:** ページを削除した場合、対応するテストケースも削除する
- **変更:** ページのURL構造やランドマーク要素のIDが変更された場合、テストを追従させる

#### 10.5.6 認証が必要なページの扱い

認証が必要なページ（マイページ等）は、テスト内で事前に認証を完了させた状態で到達性を検証する。認証フロー自体の検証はフロー検証テスト（§10.3）の責務であり、スモークテストでは認証を前提条件として処理する。

#### 10.5.7 動的ルートの扱い

動的ルート（例: `/threads/[threadId]`）は、テスト前にDBにシードデータを投入し、実在するIDでアクセスする。存在しないIDに対する404応答の検証はスモークテストのスコープ外とする。

---

## 11. 外部サービスのテスト環境対応

### 11.1 Cloudflare Turnstile

Turnstileはテスト自動化を妨げない設計とする。

| テストレベル | 対処方法 | 仕組み |
|---|---|---|
| BDD / 単体テスト | InMemoryモック | `features/support/in-memory/turnstile-client.ts` で常に成功を返す |
| 統合テスト / APIテスト | 環境変数未設定でバイパス | `turnstile-client.ts`: `TURNSTILE_SECRET_KEY` 未設定時は `return true` |
| E2E | Cloudflare公式テスト用サイトキー | `1x00000000000000000000AA`（常に成功）を使用 |

**注意:** テスト用の環境変数に本番の `TURNSTILE_SECRET_KEY` を設定してはならない。設定するとCloudflare APIへの実通信が発生し、テスト環境からのリクエストが検証失敗する。

---

## 12. テスト実行の役割分担

### 12.1 開発AI（bdd-coding）

- テストコードの作成（全レベル）
- InMemoryテストの実行（開発サイクル中の高速フィードバック）
- 単体テストの実行

### 12.2 ゲートAI（bdd-gate）

- **テストコードは書かない。実環境での実行と合否判定に特化する。**
- Supabase Local + Next.jsサーバーを起動
- 全テストスイートを実行（BDD + 統合 + API + E2E）
- 結果の判定・レポート作成（PASS/FAIL・差分分析）
- フェーズ5検証サイクルにおける最終品質ゲートとして機能する

---

## 13. CF Smokeテスト方針

### 13.1 目的

`nodejs_compat` 経由で利用するNode.js APIがCloudflare Workers Runtimeで正しく動作するかの回帰検知。ビジネスロジックのテストではなく、インフラ境界の互換性チェックに限定する。

### 13.2 検証対象

| 対象 | 検証内容 | 依存するNode.js API |
|---|---|---|
| subject.txt | Shift_JISレスポンス生成 | iconv-lite（Buffer操作） |
| DATファイル | rewriteルールの動作 | — |
| bbsmenu.html | Shift_JISレスポンス生成 | iconv-lite |
| bbsmenu.json | JSON APIの基本動作 | — |
| SETTING.TXT | Shift_JISレスポンス生成 | iconv-lite |
| bbs.cgi POST | Shift_JISデコード | Buffer.from, iconv-lite |
| Web UI SSR | SSR基本動作 | crypto（createHash等） |

### 13.3 実行環境

- **ランタイム:** `wrangler dev`（Cloudflare Workers ローカルランタイム）
- **ベースURL:** `http://localhost:8788`（wrangler devのデフォルトポート）
- **前提:** `wrangler dev` を手動で事前起動しておくこと。テスト設定にwebServer自動起動は含まない
- **テストツール:** Playwright（`cf-smoke` プロジェクト）

### 13.4 テスト件数と増減基準

- **件数:** 7件程度（固定）。機能追加に連動して増えない
- **増やす基準:** 新しいNode.js固有API（`fs`, `stream`, `zlib` 等）を使い始めた場合のみ追加を検討する
- **減らす基準:** 対象のNode.js APIを使用しなくなった場合に削除する

### 13.5 実行方法

```bash
# wrangler dev を事前に起動
npm run preview:cf

# 別ターミナルでスモークテスト実行
npm run test:cf-smoke
```

### 13.6 検証しないこと

- ビジネスロジックの正確性（BDDサービス層テスト・単体テストで検証済み）
- Cookie属性・認証フロー（APIテストで検証済み）
- DBデータの整合性（統合テストで検証済み）
- ブラウザ上のUI操作（E2Eテストで検証済み）

---

## 14. 本番スモークテスト方針

### 14.1 目的

Cloudflare Workers 本番環境が実際にユーザーに正常応答しているかを検証する。デプロイ後のリグレッション検知と、本番固有の問題（DNS、TLS、環境変数、DB接続等）の早期発見を目的とする。

### 14.2 CF Smoke（§13）との違い

| | CF Smoke（§13） | 本番 Smoke（§14） |
|---|---|---|
| 対象環境 | ローカル `wrangler dev` (localhost:8788) | Cloudflare Workers 本番環境（URL は `playwright.prod.config.ts` で定義） |
| 関心事 | Workers Runtime での Node.js API 互換性 | 本番環境の到達性・応答正常性 |
| タイミング | デプロイ**前**（ビルド検証） | デプロイ**後**（本番確認） |
| ブラウザ | 不要（API リクエストのみ） | 一部必要（ページ遷移・JS エラー検出） |

両者はテスト項目が一部重複する（同じエンドポイントを検証する）が、観点が異なる。CF Smoke はバイト列やエンコーディングの技術的正確性を検証し、本番 Smoke はページの UI 表示や JS エラー有無を検証する。

### 14.3 Phase 構成

本番スモークテストは、安全性レベルの異なる2つの Phase で構成する。

| Phase | 性質 | 実行方式 | 認証 | 本番DBへの書き込み |
|---|---|---|---|---|
| **Phase A: 到達性テスト** | 読み取り専用 | Playwright 自動実行 | 不要 | なし |
| **Phase B: 書き込みテスト** | 画面UIを通じた書き込み | 人間が認証を代行 + AI がブラウザ操作 | Turnstile + 認証コードを人間が処理 | あり（通常の掲示板利用と同等） |

### 14.4 安全性制約

**本番スモークテストは本番環境に対して実行されるため、以下の制約を厳守する。**

#### 全 Phase 共通

| 制約 | 理由 |
|---|---|
| **DB 操作ヘルパーを使用しない** | `cleanupDatabase` 等のローカル用ヘルパーは本番 DB を破壊する |
| **シードデータを投入しない** | 本番 DB に不要なテストデータが残る |
| **Supabase API を直接呼ばない** | service_role_key による本番 DB 直接操作を防止する |
| **人間の監督下でのみ実行する**（§14.8） | 万一のテスト不備による本番影響を即座に検知・停止する |

#### Phase A 固有

| 制約 | 理由 |
|---|---|
| **GET リクエストのみ** | 本番データを一切変更しない |
| **認証フローを実行しない** | Turnstile が本物であり、自動化不可 |

#### Phase B 固有

| 制約 | 理由 |
|---|---|
| **書き込みは通常の画面 UI を通じてのみ行う** | ユーザーと同じ経路を通ることで、実際の利用体験を検証する |
| **認証（Turnstile・認証コード入力）は人間が手動で行う** | BOT 検知を回避する自動化を行わない |
| **削除・管理操作はテストしない** | 本番データの破壊を防止する。必要な場合は人間が直接操作する |

### 14.5 検証対象

#### Phase A: 到達性テスト（読み取り専用・自動実行）

| # | 対象 | 検証内容 | ブラウザ要否 |
|---|---|---|---|
| A-1 | トップページ `/` | HTTP 200、サイトタイトル、スレッド作成フォーム、JS エラーなし | 要 |
| A-2 | スレッド詳細遷移 | 一覧→詳細のナビゲーション、レス表示、書き込みフォーム | 要 |
| A-3 | subject.txt | HTTP 200、Content-Type: Shift_JIS | 不要 |
| A-4 | bbsmenu.html | HTTP 200、Content-Type: Shift_JIS | 不要 |
| A-5 | bbsmenu.json | HTTP 200、Content-Type: JSON、menu_list 存在 | 不要 |
| A-6 | SETTING.TXT | HTTP 200、Content-Type: Shift_JIS | 不要 |
| A-7 | /api/threads | HTTP 200、threads 配列 | 不要 |
| A-8 | 存在しない DAT | 500 にならない（404 or 200） | 不要 |
| A-9 | 既存スレッド DAT | HTTP 200、Content-Type: Shift_JIS | 不要 |
| A-10 | /auth/verify | HTTP 200、認証フォーム表示、JS エラーなし | 要 |
| A-11 | /mypage（未認証） | 500 にならない | 要 |

#### Phase B: 書き込みテスト（人間介在・ブラウザ操作）

| # | 操作 | 確認事項 | 人間介在ポイント |
|---|---|---|---|
| B-1 | スレッド作成フォームに入力して送信 | AuthModal が表示される | — |
| B-2 | Turnstile + 認証コード入力 | 認証が完了する | **人間が Turnstile を通過し認証コードを入力** |
| B-3 | 認証完了後にスレッド作成がリトライされる | スレッドが一覧に表示される | — |
| B-4 | 作成したスレッドを開いてレスを書き込む | レスが表示される | — |
| B-5 | マイページにアクセス | 通貨残高・書き込み履歴が表示される | — |

Phase B は Playwright の自動テストスクリプトとしては実装しない。auto-debugger エージェントが Playwright MCP でブラウザを操作し、認証が必要なタイミングで人間に制御を渡す対話的な方式で実施する（auto-debugger.md の Phase 1 §1.3「人間への制御引き渡し」を参照）。

### 14.6 設定ファイル構成

```
playwright.prod.config.ts   ← 本番スモーク専用（webServer なし・Phase A のみ）
playwright.config.ts         ← ローカル専用（本番テストを含まない）

e2e/
  prod/
    smoke.spec.ts            ← Phase A: 到達性テスト（読み取り専用）
```

Phase B のテストスクリプトは存在しない。auto-debugger が対話的に実施する。

**デフォルトの `npx playwright test` では本番テストは実行されない。** config ファイルを完全に分離しているため、`--project` の指定忘れ等で本番にリクエストが飛ぶ事故を防止している。

### 14.7 実行方法

```bash
# Phase A: 到達性テスト（自動実行）
npx playwright test --config=playwright.prod.config.ts

# Phase B: 書き込みテスト（対話的実行）
# auto-debugger エージェントに「本番で書き込みテストして。認証は自分でやる」と指示する
```

### 14.8 人間監督の必須要件

**本番スモークテストは、必ず人間が監督できる状態で実行すること。** CI/CD パイプラインへの組み込みや、無人での定期自動実行は行わない。

理由:
- テストコードの不備（意図しない書き込み等）が本番に影響するリスクがある
- 本番環境の状態変化（メンテナンス、障害、仕様変更等）によりテストが誤動作する可能性がある
- Phase B では認証操作に人間の介在が必須である
- 異常を検知した際に即座にテストを停止し、状況を判断する人間が必要である

**実行が許可される状況:**
- 人間が手動でコマンドを実行する（Phase A）
- 人間が AI エージェント（auto-debugger 等）に指示して実行させ、結果を確認する（Phase A・B）

**禁止される状況:**
- GitHub Actions 等の CI/CD で自動実行する
- cron 等で定期的に無人実行する
- AI エージェントが人間の指示なく自律的に実行する

### 14.9 テスト件数と増減基準

- **Phase A 件数:** 11件（固定的。ページ追加に厳密に連動はしない）
- **Phase B 件数:** 5項目（対話的に実施。スクリプト化しない）
- **増やす基準:** 本番固有の障害が発生し、既存テストでは検知できなかった場合に追加を検討する
- **減らす基準:** 対象のページ・エンドポイントが廃止された場合に削除する

### 14.10 検証しないこと

- ビジネスロジックの正確性（BDD サービス層テスト・単体テストで検証済み）
- DB のデータ整合性（統合テストで検証済み）
- Workers Runtime の Node.js API 互換性（CF Smoke テスト §13 で検証済み）
- 管理画面の操作（本番データ破壊リスクが高いため、人間が直接確認する）
